import 'package:flutter/material.dart';

class ConstraintsApp extends StatelessWidget {
  const ConstraintsApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
            width: 100,
            height: 100,
            color: Colors.red,
      ),
    );
  }
}
